
node_modules/
dist/
*.db
*.db-shm
*.db-wal

.env
.env.*
!.env.example

npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

.DS_Store
Thumbs.db

test.js

# Rust
rust/target/
rust/target-check/

# Claude Code local
.claude/settings.local.json

# Harness per-session state (ephemeral, one file per session_id)
harness/state/

# Old Node leftovers (repo root only)
/package.json
/package-lock.json
/tsconfig.json

# DB backups (la DB misma ya está ignorada arriba)
*.db.bak-*

# Copias de respaldo puntuales de scripts
*.bak

# Puntero sesión → tarea del harness de fases (efímero, uno por session_id)
.harness/current/
